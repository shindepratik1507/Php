//Q2. Write a PHP program to define Interface shape which has two method as area() and
volume(). Define a constant PI. Create a class Cylinder implement this interface and
calculate area and Volume. //



<?php
// Define an interface 'Shape' with two methods: area() and volume()
interface Shape {
    public function area();
    public function volume();
}

// Define a class 'Cylinder' that implements the 'Shape' interface
class Cylinder implements Shape {
    const PI = 3.1416; // Define a constant PI

    private $radius;
    private $height;

    // Constructor to initialize radius and height
    public function __construct($radius, $height) {
        $this->radius = $radius;
        $this->height = $height;
    }

    // Method to calculate the surface area of the cylinder
    public function area() {
        // Surface area of a cylinder = 2πr(r + h)
        return 2 * self::PI * $this->radius * ($this->radius + $this->height);
    }

    // Method to calculate the volume of the cylinder
    public function volume() {
        // Volume of a cylinder = πr²h
        return self::PI * pow($this->radius, 2) * $this->height;
    }
}

// Instantiate the Cylinder class with a radius and height
$cylinder = new Cylinder(5, 10); // For example, radius = 5, height = 10

// Display the area and volume of the cylinder
echo "Cylinder Area: " . $cylinder->area() . " square units<br>";
echo "Cylinder Volume: " . $cylinder->volume() . " cubic units<br>";
?>